Program pruebas;

Type 
    PInt = ^Integer; // Puntero a entero
    PBool = ^Boolean;  // Puntero a booleano

Var 
    PNumero: PInt;  // Variable del tipo puntero a entero
    // Pnumero almacenará la dirección de memoria de un entero
    

Begin 
End. 